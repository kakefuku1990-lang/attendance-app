## 勤怠管理アプリ 要件定義

### 1. 概要

- **システム名（仮）**: 勤怠管理アプリ
- **目的**: 社員の日々の出退勤・休暇・残業などの勤怠情報を一元管理し、管理部門および上長の承認・集計業務を効率化する。
- **利用技術**
  - **バックエンド**: Python / Django
  - **フロントエンド**: Django標準テンプレート（将来SPA化も考慮）
  - **データベース**: PostgreSQL
  - **インフラ/環境構築**: Docker / docker-compose

### 2. 想定利用者・ロール

- **一般社員**
  - 自身の出退勤打刻
  - 打刻修正申請
  - 休暇申請・残業申請
  - 自身の勤怠実績・残有給などの確認
- **上長（マネージャー）**
  - 部下の勤怠実績閲覧
  - 休暇／残業／打刻修正の承認・却下
  - チーム単位の勤怠レポート参照
- **管理者（人事・総務・システム管理者）**
  - 組織・部署・ユーザーのマスタ管理
  - 勤怠ルール（就業時間、休憩時間、休日、残業ルールなど）の設定
  - 全社レポートの出力
  - 権限管理・アカウントロック/有効化

### 3. 対象スコープ（初期リリース）

- **対象範囲**
  - 出退勤打刻（出勤・退勤・休憩開始・休憩終了）
  - 勤怠修正申請と承認
  - 休暇申請（有給、特別休暇など）と承認
  - 残業申請と承認
  - 日次・月次の勤怠実績閲覧
  - ユーザー・部署・ロール管理
- **除外（今後の拡張候補）**
  - 給与計算との直接連携
  - 打刻用ハードウェア（ICカードリーダーなど）との連携
  - 外部SaaSとのSSO連携

### 4. 機能要件

#### 4.1 認証・認可

- **ログイン／ログアウト**
  - Django標準の認証をベースに、メールアドレス or 社員番号でのログイン。
  - パスワードリセット（メール送信 or 管理者による再発行）。
- **権限ロール**
  - 一般社員、上長、管理者の3ロールを基本とし、ロールによってアクセス可能画面・操作を制御。

#### 4.2 出退勤打刻

- **打刻機能**
  - 出勤、退勤、休憩開始、休憩終了をWeb画面から登録。
  - 当日の打刻状況を表示（例: 「出勤済み／休憩中／退勤済み」）。
  - 二重打刻の防止（状態に応じたボタン制御）。
- **打刻履歴**
  - 過去の打刻記録を日別に閲覧可能。

#### 4.3 勤怠修正申請

- **修正申請**
  - 打刻漏れや誤打刻があった場合、対象日・時刻・理由を入力して申請。
  - 上長が承認・却下可能。
- **履歴管理**
  - 修正前後の値、申請理由、承認者、承認日時を保持。

#### 4.4 休暇申請機能

- **申請種類**
  - 有給休暇、半休、特別休暇などを種別として管理。
- **申請フロー**
  - 申請：対象日・種別・理由を入力。
  - 承認：上長が承認／却下。結果を申請者に通知。
- **残日数管理**
  - 有給残日数の表示（簡易なロジックから開始し、詳細な計算は将来拡張）。

#### 4.5 残業申請機能

- **申請項目**
  - 日付、予定残業時間、理由。
- **承認フロー**
  - 上長が承認／却下。
  - 承認済み残業は実績と紐づけて管理。

#### 4.6 勤怠閲覧・レポート

- **一般社員向け**
  - 自分の月次勤怠一覧（各日ごとの出勤・退勤・実働時間・残業時間・休暇種別など）。
- **上長向け**
  - 部下の勤怠一覧（月次／日次）。
  - ステータスフィルタ（承認待ち申請、承認済み、却下など）。
- **管理者向け**
  - 部署・全社単位での集計（総労働時間、残業時間、休暇取得数など）をCSV等でエクスポート。

#### 4.7 マスタ管理

- **ユーザー管理**
  - ユーザー作成・編集・無効化（退職者など）。
  - 所属部署、上長、ロールの設定。
- **部署管理**
  - 部署マスタ（部署名、上位部署など）。
- **勤怠ルール管理（初期は最小限）**
  - 所定勤務時間（例: 9:00〜18:00）。
  - 休憩時間帯（例: 12:00〜13:00）。
  - 休日カレンダー（会社共通の祝日定義）。

#### 4.8 通知機能（初期は最小構成）

- **通知対象**
  - 承認依頼（上長への通知）。
  - 承認結果（申請者への通知）。
- **手段**
  - 初期は画面上の通知一覧（メール連携は拡張候補）。

#### 4.9 ログ・監査

- **操作ログ**
  - ログイン／ログアウト。
  - 申請・承認・却下・修正などの重要操作。
- **監査用情報**
  - 誰が・いつ・何を変更したかをトレースできるレベルで保存。

### 5. 非機能要件

- **パフォーマンス**
  - 同時アクセス数は小規模（〜数十ユーザー）を想定。
  - 一般的な操作で3秒以内の応答を目標。
- **可用性**
  - 初期は単一構成（Dockerコンテナ単位）を想定。
- **セキュリティ**
  - HTTPS 前提の運用（本番環境）。
  - パスワードはハッシュ化保管（Django標準）。
  - CSRF対策、SQLインジェクション対策（Django標準機能を活用）。
- **バックアップ**
  - PostgreSQL データの定期バックアップ方針（例: 1日1回ダンプ取得）。
- **ログ管理**
  - アプリケーションログ／DBログの保存場所とローテーション方針を検討。

### 6. システム構成（案）

- **コンテナ構成**
  - `web` コンテナ: Djangoアプリ（gunicorn + nginx または開発時はrunserver）。
  - `db` コンテナ: PostgreSQL。
  - 将来的に `worker`（非同期バッチなど）コンテナを追加可能にする。
- **環境**
  - `development`: ローカルでの開発用（docker-composeで起動）。
  - `staging`（任意）: 本番前検証環境。
  - `production`: 本番運用環境（将来拡張）。

### 7. 使用技術スタック（詳細）

- **言語／フレームワーク**
  - Python 3.x
  - Django 4.x 以降（LTS版を優先）
- **DB**
  - PostgreSQL 14 以降（想定）
- **その他ライブラリ（候補）**
  - 認証・権限: Django標準 + カスタムUserモデル
  - API提供を見据える場合は Django REST Framework（DRF）を採用候補。

### 8. 画面一覧（ラフ）

- **共通**
  - ログイン画面
  - ダッシュボード（ロールに応じて表示内容を変更）
- **一般社員向け**
  - 当日打刻画面
  - 自分の勤怠一覧（月次）
  - 休暇申請画面
  - 残業申請画面
  - 打刻修正申請画面
- **上長向け**
  - 承認待ち申請一覧（休暇／残業／修正）
  - 部下の勤怠一覧
- **管理者向け**
  - ユーザー管理画面
  - 部署管理画面
  - 勤怠ルール設定画面
  - レポート出力画面

### 9. データモデル（概略）

- **User**
  - id, 氏名, メールアドレス, 社員番号, ロール, 部署, 上長, 在籍状態 など。
- **Department**
  - id, 部署名, 上位部署。
- **AttendanceRecord**
  - id, user, 日付, 出勤時刻, 退勤時刻, 休憩開始・終了時刻, 実働時間, 残業時間 など。
- **AttendanceCorrectionRequest**
  - id, 対象user, 対象日, 修正前後の値, 理由, ステータス, 申請日時, 承認者, 承認日時。
- **LeaveRequest**
  - id, user, 日付 or 期間, 休暇種別, 理由, ステータス, 承認者。
- **OvertimeRequest**
  - id, user, 日付, 予定残業時間, 理由, ステータス, 承認者。
- **HolidayCalendar**
  - id, 日付, 名称, 種別（祝日／会社休日など）。

---

## フェーズ別実装ロードマップ（学習用）

> ※このロードマップは、上記の要件定義を **最終ゴールとして固定** したうえで、  
> 「学習効率」と「小さな成功体験」を優先して段階的に進めるためのものです。  
> 各フェーズは必ず `docker compose up` で動く状態をゴールにします。

---

### フェーズ0：開発環境の最小セットアップ（Hello Django）

- [○] **タスク0-1: プロジェクトディレクトリ＆Git初期化**
  - **目的（何を学ぶか）**: プロジェクトの基本的なディレクトリ構成と、Gitでの変更履歴管理の流れを理解する。
  - **完了条件（確認方法）**
    - `attendance_mg` 配下にソースコード用のディレクトリができている。
    - `git init` 済みで、`git status` がクリーン。
- [○] **タスク0-2: Dockerで「何もしない」コンテナを動かす**
  - **目的**: Dockerの基本（`Dockerfile` / `docker-compose.yml` / `docker compose up`）の流れに慣れる。
  - **完了条件**
    - シンプルな `web` サービスのみの `docker-compose.yml` で、`alpine` や `python` ベースのコンテナが `docker compose up` で起動する。
    - `docker compose ps` でコンテナの状態が確認できる。
- [○] **タスク0-3: Django入りコンテナで「Hello, Django」ページ表示**
  - **目的**: コンテナ内でDjangoを動かし、ブラウザにページを表示する一連の流れを学ぶ。
  - **完了条件**
    - `web` コンテナの `Dockerfile` にDjangoインストールと起動コマンドを記述できている。
    - コマンド例:  
      - `docker compose build`  
      - `docker compose up -d`  
      - `docker compose exec web django-admin startproject config .`（または類似）  
    - ブラウザで `http://localhost:8000` にアクセスすると、Djangoの初期画面 or 独自の「Hello, Django」ページが表示される。

---

### フェーズ1：PostgreSQL導入＋Django管理画面ログイン（DB＋adminの基礎）

- [○] **タスク1-1: `db`（PostgreSQL）サービスの追加**
  - **目的**: Docker Composeで複数コンテナ（web + db）を連携させる方法を学ぶ。
  - **完了条件**
    - `docker-compose.yml` に `db` サービス（PostgreSQL）が定義されている。
    - 環境変数でDB名・ユーザー・パスワードを設定できている（`.env` 利用）。
    - `docker compose up -d` で `web` と `db` の両方が起動している。
- [○] **タスク1-2: DjangoからPostgreSQLへ接続する設定**
  - **目的**: `settings.py` でのDB設定と環境変数の使い方を学ぶ。
  - **完了条件**
    - `settings.py`（または分割した設定ファイル）で `ENGINE = "django.db.backends.postgresql"` を指定。
    - ホスト名に `db`（Composeのサービス名）を指定。
    - コマンド例:  
      - `docker compose exec web python manage.py migrate`
    - マイグレーションが成功し、エラーなく完了する。
- [○] **タスク1-3: Django管理ユーザー（superuser）作成＆ログイン**
  - **目的**: Django adminの基本操作と、コンテナ内での管理コマンド利用に慣れる。
  - **完了条件**
    - コマンド例:  
      - `docker compose exec web python manage.py createsuperuser`
    - ブラウザで `http://localhost:8000/admin/` にアクセスし、superuserでログインできる。

---

### フェーズ2：最低限のユーザー運用＋トップページ（ログイン状態の確認）

- [○] **タスク2-1: シンプルなトップページ作成（テンプレート＋ビュー）**
  - **目的**: DjangoのURLルーティング・ビュー・テンプレートのつなぎ方を理解する。
  - **完了条件**
    - `config/urls.py` からトップページ用のビューにルーティングされる。
    - テンプレートで「勤怠管理アプリ（仮）」のタイトルが表示される。
    - `docker compose up -d` の後、`http://localhost:8000/` でトップページが表示される。
- [○] **タスク2-2: Django標準ログイン／ログアウト導入**
  - **目的**: Djangoの認証フレームワークの基本的な使い方を学ぶ。
  - **完了条件**
    - `django.contrib.auth` のログインビュー／ログアウトビューをURLに紐付け。
    - シンプルなログインフォーム画面を表示できる。
    - ログイン成功後、トップページにリダイレクトされ、ヘッダーなどに「〇〇さんでログイン中」と表示される。
- [○] **タスク2-3: adminで一般ユーザーを追加し、ログイン確認**
  - **目的**: 「管理者でユーザーを作成 → 一般ユーザーでログイン」という実運用に近い流れを体験する。
  - **完了条件**
    - Django adminで一般ユーザーを1人追加。
    - そのユーザーでログイン／ログアウトできる。

---

  ### フェーズ3（MVP）：打刻＋自分の打刻一覧（最初のゴール）

  > ここが **最初の大きなゴール（MVP）** です。  
  > 「ログインしたユーザーが自分の出退勤を記録し、自分の履歴を一覧で見られる」状態を目指します。

  - [○] **タスク3-1: `attendance` アプリ作成**
    - **目的**: Djangoアプリを追加し、勤怠関連のコードの置き場所を分ける設計を学ぶ。
    - **完了条件**
      - コマンド例: `docker compose exec web python manage.py startapp attendance`
      - `INSTALLED_APPS` に `attendance` が追加されている。
  - [○] **タスク3-2: `AttendanceRecord` モデル作成（最小構成）**
    - **目的**: モデル設計とマイグレーションの基本サイクルを学ぶ。
    - **（最小構成の例）**
      - `user`（外部キー）  
      - `date`（日付）  
      - `clock_in`（出勤時刻、nullable）  
      - `clock_out`（退勤時刻、nullable）
    - **完了条件**
      - モデル定義 → マイグレーション作成 → マイグレーション適用ができている。  
        コマンド例:  
        - `docker compose exec web python manage.py makemigrations`  
        - `docker compose exec web python manage.py migrate`
      - Django adminに `AttendanceRecord` が表示され、管理画面からレコードを追加できる。
  - [○] **タスク3-3: 当日打刻画面（出勤／退勤ボタン）の実装**
    - **目的**: ログインユーザーに紐づくレコードを操作する基本を学ぶ。
    - **完了条件**
      - ログイン必須のURLとして「今日の打刻ページ」がある。
      - 「出勤」ボタン押下で、本日分の `AttendanceRecord` の `clock_in` が現在時刻で登録される（なければ新規作成）。
      - 「退勤」ボタン押下で、本日分の `clock_out` が現在時刻で登録される。
      - `docker compose up -d` で起動後、ブラウザから実際にボタンを押してDBにレコードが作られていることをadminで確認できる。
  - [○] **タスク3-4: 自分の打刻一覧画面（簡易版）の実装**
    - **目的**: クエリセットでのフィルタリングと、テンプレートでの一覧表示を学ぶ。
    - **完了条件**
      - ログインユーザー自身の `AttendanceRecord` を日付順で一覧表示するページがある。
      - まだ「実働時間」「残業時間」などは計算しなくてよい（あとで拡張）。
      - ブラウザでアクセスし、打刻した履歴がテーブルで確認できる。

  ---

### フェーズ4：ユーザー情報の拡張（社員番号・部署などの導入）

- [○] **タスク4-1: カスタムUserモデルへの移行（必要に応じて）**
  - **目的**: DjangoのカスタムUserモデルの考え方と、初期段階での設計の重要性を学ぶ。
  - **完了条件**
    - 社員番号・部署などを持つカスタムUserモデル（または `Profile` 的な拡張モデル）が導入されている。
    - `docker compose exec web python manage.py migrate` が通る。
    - adminで社員番号や部署を編集できる。
- [○] **タスク4-2: `Department` モデルの追加とUserとの紐付け**
  - **目的**: マスタテーブルとユーザーのリレーション設計を学ぶ。
  - **完了条件**
    - `Department` モデルを定義し、Userと外部キー／多対一で紐付け。
    - adminから部署の作成・編集・削除ができる。
    - ユーザーに部署を紐付けられる。

---

### フェーズ5：申請ワークフロー（修正・休暇・残業）のシンプル版

- [○] **タスク5-1: 勤怠修正申請の最小実装**
  - **目的**: 申請と承認という2段階のワークフローの基本を学ぶ。
  - **完了条件**
    - `AttendanceCorrectionRequest` の簡易モデルを作成（対象日・修正内容・理由・ステータス程度）。
    - 自分用の「修正申請画面」から申請が作れる。
    - adminでステータスを「承認済み」に変更し、その結果を `AttendanceRecord` に反映する処理が実装されている。
- [○] **タスク5-2: 休暇申請の最小実装**
  - **目的**: 日付や区間を扱うモデル・フォーム実装を学ぶ。
  - **完了条件**
    - `LeaveRequest` モデルを簡易に定義（単日有給など、まずはシンプルなケースに限定）。
    - 休暇申請画面から申請が作れ、adminで承認ステータスの変更ができる。
- [○] **タスク5-3: 残業申請の最小実装**
  - **目的**: 時間数や予定残業を扱うモデルと、その表示・編集を学ぶ。
  - **完了条件**
    - `OvertimeRequest` モデルを定義し、申請画面から登録できる。
    - adminで承認／却下を切り替えられる。

---

### フェーズ6：上長画面・簡易承認UI・マスタ管理

- [○] **タスク6-1: ロール（一般社員／上長／管理者）による画面出し分け**
  - **目的**: 権限によって表示内容やアクセスを切り替える方法を学ぶ。
  - **完了条件**
    - Userにロール情報を持たせる（フィールド or グループ）。
    - 一般社員は自分の勤怠・申請のみを閲覧可能。  
      上長は部下の勤怠・申請を閲覧可能。
- [○] **タスク6-2: 上長向け承認画面（一覧＋承認ボタン）**
  - **目的**: 「管理者権限なしでの承認フローUI」を作る方法を学ぶ。
  - **完了条件**
    - ログインした上長ユーザーが、部下の申請一覧を見られる。
    - 画面上のボタン操作で「承認／却下」を行える（adminに入らずに）。
- [○] **タスク6-3: 部署・休日カレンダーなどマスタ管理の画面化（必要な範囲で）**
  - **目的**: システム管理者が使うマスタ編集UIを実装する体験をする。
  - **完了条件**
    - 部署マスタ・休日カレンダーなどの最低限のマスタを、画面から管理できる。

---

### フェーズ7：レポート・集計・非機能対応

- [○] **タスク7-1: 月次勤怠レポート（簡易版）**
  - **目的**: 集計クエリとテンプレートでの表示を学ぶ。
  - **完了条件**
    - 指定月の総労働時間・残業時間を簡単に集計して表示する画面がある。
- [○] **タスク7-2: CSVエクスポート（簡易版）**
  - **目的**: DjangoからCSVを生成・ダウンロードさせる方法を学ぶ。
  - **完了条件**
    - 部署単位 or 全社単位の勤怠一覧をCSVでダウンロードできる。
- [○] **タスク7-3: ログ・バックアップ・README整備**
  - **目的**: 実運用を意識した非機能（ログ・バックアップ・ドキュメント）を学ぶ。
  - **完了条件**
    - 簡易的なアプリケーションログ出力設定がある。
    - PostgreSQLの `pg_dump` などを使ったバックアップコマンド例をメモしている。
    - `README.md` に以下が記載されている：  
      - 環境構築手順  
      - 主要な `docker compose` コマンド  
      - 初回セットアップ手順（`migrate` / `createsuperuser` など）

